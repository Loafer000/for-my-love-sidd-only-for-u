 # MongoDB Connection String (replace with your own from MongoDB Atlas)
 MONGODB_URI="mongodb+srv://<user>:<password>@<cluster-url>/spaceconnect?retryWrites=true&w=majority"
 
 # Server Port
 PORT=3000
 
 # Node Environment
 NODE_ENV=development